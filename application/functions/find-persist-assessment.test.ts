test('gets markets for symbol on any list of exchanges exchanges', () => {
//   const markets = getMarketsForSymbolOnExchanges('BTC/LTC', exchangeClient, 'kraken', '')
})

test('assesses arbitrage opportunity between on symbol between two exchanges', () => {
  
})

test('persists every assessment', () => {
  
})
