const { PubSub } = require('@google-cloud/pubsub')
const { serialize, exchangeIds, topics, symbols } = require('arbitrage-lib')

const pubsub = new PubSub()

exports.opportunity = async () => {
    
}
