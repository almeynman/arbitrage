{
  "name": "rate-persistor",
  "version": "0.0.1",
  "main": "src/index.js",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "test": "ava -T 30s test/*.test.js",
    "simulate-minute-tick": "gcloud pubsub topics publish minute-tick --message minute-tick",
    "simulate-fetch-rate": "gcloud pubsub topics publish fetch-rates-for-exchange --message '{\"exchange\":\"bitfinex\"}'",
    "deploy-minute-tick-processor": "gcloud functions deploy minuteTickProcessor --trigger-topic minute-tick --runtime=nodejs10 --region=europe-west1",
    "deploy-fetch-rate-processor": "gcloud functions deploy fetchRateForExchangeProcessor --trigger-topic fetch-rates-for-exchange --runtime=nodejs10 --region=europe-west1",
    "package-arbitrage-lib": "cd ../arbitrage-lib && npm version 0.0.1 --allow-same-version && npm pack && mv arbitrage-lib*.tgz ../rate-persistor/ && cd ../rate-persistor && npm install file:arbitrage-lib-0.0.1.tgz",
    "deploy": "npm run package-arbitrage-lib && npm run deploy-minute-tick-processor && npm run deploy-fetch-rate-processor"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.28.1",
    "arbitrage-lib": "../arbitrage-lib"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^3.2.0",
    "ava": "^1.4.1",
    "proxyquire": "^2.1.0",
    "sinon": "^7.3.2"
  }
}
