{
  "name": "business-logic",
  "version": "1.0.1",
  "main": "build/index.js",
  "scripts": {
    "lint": "tsc --noEmit && eslint . --ext ts --quiet --fix",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf ./build"
  },
  "dependencies": {
    "ts-lib-common": "1.0.1"
  }
}
