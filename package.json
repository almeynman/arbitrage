{
  "name": "arbitrage-lib",
  "version": "0.0.1",
  "main": "src/index.js",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "test": "ava -T 30s test/*.test.js --verbose",
    "deploy": "gitpkg publish"
  },
  "dependencies": {
    "ava": "^1.4.1",
    "safe-buffer": "^5.1.2"
  },
  "devDependencies": {
    "gitpkg": "^1.0.0-beta.1"
  }
}
