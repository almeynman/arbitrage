{
  "name": "arbitrage",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=10",
    "npm": ">=6.9.0"
  },
  "scripts": {
    "clean": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "boot": "lerna exec npm install",
    "lint": "eslint --format=codeframe '**/*.js'",
    "pretest": "npm run lint",
    "test": "lerna run test --stream",
    "update-version": "lerna publish --skip-git --skip-npm --force-publish '*'",
    "check-packages": "yarn clean && yarn compile && yarn test && yarn lint",
    "deploy": "yarn update-version && lerna run deploy",
    "lerna-wizard": "npx lerna-wizard",
    "upgrade-deps": "yarn upgrade-interactive --latest"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-monorepo": "^0.2.1",
    "eslint-plugin-node": "^8.0.1",
    "eslint-plugin-prettier": "^3.0.1",
    "husky": "^1.3.1",
    "lerna": "^3.13.3",
    "lint-staged": "^8.1.5",
    "prettier": "^1.17.0"
  },
  "dependencies": {
    "arbitrage-lib": "file:packages/arbitrage-lib",
    "minute-tick-processor": "file:packages/minute-tick-processor"
  },
  "lint-staged": {
    "*.js": [
      "eslint --format=codeframe"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
